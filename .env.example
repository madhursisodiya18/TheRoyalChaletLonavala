# Copy to .env and fill in (do not commit .env).
# Flask
SECRET_KEY=
FLASK_DEBUG=false
# Gunicorn (production): WEB_CONCURRENCY=2 WEB_THREADS=4

# Email (contact / password reset)
MAIL_USERNAME=
MAIL_PASSWORD=
